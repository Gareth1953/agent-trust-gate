# Agent Discovery Indexing Pack
Creates local notes and metadata for humans, LLMs, developers, and future agents. It never submits, crawls, scrapes, shares, or fetches. Static discovery remains read-only; all operational capabilities remain disabled.
