# Static Global Developer Landing Page

P3-M106 prepares `public/index.html` as a static local file for possible future public developer visibility. It explains the product, local proof chain, code-readable assets, safety boundaries, and omnichannel direction without creating a service.

The page is not deployed and not hosted. It contains no analytics, tracking, external network calls, forms, agent contact, payment activation, or settlement activation. It loads only the local `public/styles.css` file and repository-relative content links.

## Local preview

Open `public/index.html` directly in a browser. No HTTP server, build step, network connection, or deployment command is required.

The preview remains `local_demo_only`. AUC is not integrated. Agent Contact System is not integrated.

**No mandate. No evidence. No verified intent. No signed gate pass. No settlement.**
