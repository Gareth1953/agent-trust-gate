# Future Mission Advantage Review Template

This is a local planning template. Complete it with non-sensitive information and human-reviewed assumptions. It does not authorize implementation or live activation.

## Mission

- `mission_id`:
- `mission_name`:
- `proposed_feature`:
- `problem_it_solves`:
- `why_agents_would_use_it`:
- `why_machines_would_pay_for_it`:
- `how_it_strengthens_refusalgraph`:
- `how_it_strengthens_agent_clearing`:

## Risks

- `private_data_risk`:
- `live_activation_risk`:
- `competitor_overlap_risk`:
- `required_safety_blocks`:

## Scores

- `uniqueness_score` (`0–10`):
- `defensibility_score` (`0–10`):
- `agent_to_agent_value_score` (`0–10`):
- `machine_to_machine_fee_potential_score` (`0–10`):
- `refusalgraph_value_score` (`0–10`):
- `private_data_safety_score` (`0–10`):
- `developer_adoption_score` (`0–10`):
- `commercial_clarity_score` (`0–10`):
- `big_company_resistance_score` (`0–10`):
- `simplicity_score` (`0–10`):
- `fee_potential_score` (copy the machine-to-machine fee score for review visibility):
- `overall_advantage_score`:

## Recommendation

- `recommendation`: `reject_or_defer | improve_before_building | build_if_safe | high_priority | strategic_priority`
- score rationale and uncertainties:
- evidence required before implementation:
- conditions requiring re-score:

## Approval

- `Gareth final approval required`: **true**
- Gareth decision: `not_reviewed | approved_for_local_build | deferred | rejected`
- live/commercial activation: **disabled**

No approval in this template enables scanning, scraping, lookup, publishing, deployment, outreach, analytics, tracking, payment, billing, settlement, fees, third-party connections, or action execution.
